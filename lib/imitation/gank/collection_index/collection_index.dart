import 'package:flutter/material.dart';

class CollectionIndex extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('GankIO收藏页'),
    );
  }
}