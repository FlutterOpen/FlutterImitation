import 'package:flutter/material.dart';

class PublicIndex extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('WanAndroid公众号页'),
    );
  }
}