import 'package:flutter/material.dart';

class ProjectsIndex extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('WanAndroid项目页'),
    );
  }
}