import 'package:flutter/material.dart';

class IndexIndex extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('WanAndroid首页'),
    );
  }
}